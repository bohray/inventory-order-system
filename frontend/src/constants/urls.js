// Central registry of frontend route paths. Import these instead of
// hardcoding route strings in the router, navigation, or links.

export const urls = {
  root: "/",
  dashboard: "/dashboard",
  products: "/products",
  customers: "/customers",
  orders: "/orders",
};
